import type { Schema, FieldsParam, Entity, ListParams } from "../../types"
import { fields, queryParams } from "../../utils"
import { ResourceApi } from "../common"

export class DatabaseBackupSettingsApi extends ResourceApi {
  // TODO
}